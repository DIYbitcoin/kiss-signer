# One line per locale: the number of overlapcheck findings it may have.
#
# SHRINK ONLY. The runner refuses any number that goes UP, and prints a
# nudge for any that has come down. English is deliberately absent: it has
# no allowance and the push lane keeps it at zero. See the ceiling block in
# sim/run_overlapcheck.sh for why the translations get a number at all.
#
# THESE NUMBERS WENT UP ONCE, ON PURPOSE, and this block is the "say so in
# the commit" the runner asks for. Every number here except the German
# one was measured on a walk that DIED before the end. A dead walk prints "clean"
# for every stop it never reached, so the old ceilings were not ceilings on
# what a locale renders -- they were ceilings on the fraction of the walk it
# survived. Twenty locales could not finish one until the crash, the
# ambiguous needles, the ellipsised row name, the fullwidth colon and the
# never-sparse file list were all out of the way.
#
# So these are the first honest measurement, not a regression: the same
# screens, counted to the end for the first time. German is the control --
# it always finished, and it is the only line that went DOWN, 103 to 100.
#
# AND THEY WENT UP A SECOND TIME, for a different reason worth separating
# from the first. The numbers above were measured BY HAND, because the
# nightly sweep that is supposed to keep them honest had never once run: both
# locale workflows died twenty seconds in on an LVGL clone that existed in
# one other workflow. With that fixed, the first complete twenty one locale
# sweep put ten locales two over -- Turkish four -- and none under.
#
# The two are this session's screens read in translation, not a defect: the
# SIGNER and DEVICE tabs' rows, the card page's single door, the pairing head
# moved onto the chrome strip, and the import note, whose fit budget said 190
# against a 108px box and so overflowed the line below it silently in ten
# languages. Correcting that trades a silent overlap for an honest "this copy
# does not fit", which is a translation cut and waits for the sweep.
#
# English stays 0 and is still the only hard zero here.
#
# They are a ratchet from here and the rule is unchanged. The next number
# that moves goes down.
#
# AND THEY CAME DOWN, sixteen at once, which is the direction this file is
# for. Measured on a full walk of every Latin-script locale after the band
# controls were lifted above their own fill and after payment was swept out
# of those sixteen. Every one came in under, by between 5 and 29 findings,
# so these are the honest numbers again rather than the allowance left over
# from the screens that earned them.
#
# Japanese, Korean, Russian and Chinese were measured next, after payment
# came out of those four as well. Japanese, Korean and Chinese landed exactly
# on their existing numbers and keep them. Russian came in five under and
# takes the lower one. All twenty are now the measurement rather than an
# allowance, which is the first time that has been true.
cs-CZ 7
da-DK 15
de 30
es-ES 27
es-MX 27
fr 28
hr-HR 15
it 15
ja 10
ko 4
nb-NO 12
nl 33
pl 24
pt-BR 15
pt-PT 18
ru 31
sv-SE 24
tr 14
vi 15
zh-CN 12
