# build output (idf.py -B <dir>); sdkconfig.release is regenerated by
# tools/build_release.sh on every run
build*/
sdkconfig.release
sdkconfig.old

# IDF component manager downloads these from idf_component.yml +
# dependencies.lock (which IS tracked)
managed_components/

# local tooling / OS noise
.claude/
docs/superpowers/
.DS_Store
__pycache__/
*.pyc
